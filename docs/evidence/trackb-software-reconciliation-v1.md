# Track B Software Reconciliation v1

Software implementation and automated verification complete. Manual Safari acceptance remains pending. No merge to main was performed.

1. **Branch and isolation.** `trackb-reconcile`, in `/private/tmp/sm2010-trackb-reconcile`. Preflight on that branch reported a clean working tree and HEAD `91b9678341cb9e6a21b0ad5cc17cdbe645f3b3a0`. Original worktrees and branch tips are preserved: `main` at that hash and `hero-shell` at `db3ace920aa6080f6d860030d91f5fd7572af8f7`.

2. **Starting authority.** Current main content wins. The canonical clock remains October 20, 2010, 00:02–00:17, America/Los_Angeles. Historical records, geography/maps, public Twitter fixtures/repository, shared character media, canonical documentation, package manifests and the normal `src/main.tsx` entry are unchanged.

3. **DeviceScreen.** Audited extraction `f2267c0` was applied without committing, then passed build, seed validation and whitespace checks before checkpoint `ec9ddc3`. Mechanical mount, ordering, provider, viewport and runtime-owner assertions passed. The normal `<App />` entry remains; no presenter selection was added.

4. **Software foundations.** Live timeline labels derive from canonical start plus event offsets. Facebook delivery metadata uses event due time instead of scheduler-observation time. T+890 is `2010-10-20T07:16:50.000Z` / `12:16 AM`. Foursquare relative time uses current simulated time without changing its data or layout. DeviceAudio defaults to enabled and provides `setMuted`, `canPlayAudio`, `bindAudioMode`, and `audioModeChanged`; active sound stops on mute and skipped sounds do not replay. Camera selection and Roll bootstrap are keyed to experienceSessionId and deduplicate effect replay. The existing software shutdown/reset boundary clears session resources.

5. **Notifications.** One App-owned controller handles FIFO presentation, session-local unread badges, delivery claims and reset. Existing SMS adapters and period lock/alert surfaces remain. Facebook directed events and conservative Foursquare delivery are enabled; Twitter external push is disabled and Instagram push remains HOLD. Foreground app suppression, keyboard/system priority and DeviceAudio-only delivery are tested. Low battery stays outside the app queue.

6. **Media.** One App-owned request supports Messages, Facebook and Twitter, Camera/library/source choice, retained requester context and drafts, cancellation, and selected Roll resource references. All requesters share one Camera preview/runtime. Messages SEND creates one outgoing image with canonical time and existing preview semantics. Facebook SUBMIT_STATUS accepts photo-only and text+photo. Twitter SUBMIT_NEW_TWEET accepts text+photo, requires text and retains 140-character behavior. Pending media clears after success/reset. MMS retains the green outgoing family, 3px inset, right alignment, restrained tail and source aspect ratio. No posting-unavailable copy or extra photo-area Cancel was imported.

7. **Harness adaptation.** `softwareSession.test.mjs` replaces physical lifecycle callbacks with the normal name form, software Power/boot, lock/unlock and shutdown controls. It preserves two-session notification, badge, queue, foreground, low-battery, sleep, Camera and timer assertions. `mediaAttachmentFlow.test.mjs` uses the same normal App and real container callbacks, software Home/Power, terminal battery shutdown and existing public-post outro. Camera session IDs are inspected through software media diagnostics. No App presenter prop, physical controller or portal is loaded. Keyboard tests cover three requesters × three media stages × two loops. A cross-session unresolved-capture test additionally proves old work cannot persist into, block, or release the capture lock of a new run. All new hooks remain above the identity screen's early return.

8. **Validator union.** Main's seed, historical content, Foursquare, geography/maps and canonical timeline assertions remain. Superseded extraction/picker/tool assertions now describe the single shared Camera surface and media controls. Added checks enforce one App/session/Camera/media/audio owner, session-keyed selection/bootstrap, software reset and stale-capture guards, existing publishing pipelines, MMS style, no engineering copy, and no physical presenter dependency. Notification ownership/audio/reset/priority assertions include rejecting mutations. No physical lifecycle, ScreenPortal, hardware or 3D assertions were imported.

9. **Deliberately excluded.** No wholesale merge or mixed-commit cherry-pick. Excluded `src/hero/**`, `src/assets/hero/**`, `scripts/blender/**`, `hero.html`, DeviceRoot, DevicePresentation, screen diagnostics/presenter selection, physical lifecycle tests, hardware mute bindings, lighting/halo/spill, 3D adapters and physical-model evidence. Main's newer narrative/Foursquare/content files were not replaced by intermediate source versions. Existing legacy `Session.phase === "hero"` and `returnToHeroPending` names still describe main's software identity/reset flow; they introduce no dependency on physical presentation code.

10. **Forbidden-import audit.** Searches of source/tests found no HeroController, HeroSandbox, ScreenPortal, DevicePresenter, `@react-three`, Three.js import or `/hero/` import. The validator also enforces that boundary. Package files remain identical to main, so no presentation dependencies were added.

11. **Diff scope.** Review with `git diff main...trackb-reconcile --name-only` and `git diff main...trackb-reconcile --stat`. Changes are software controllers, presentation, state, audio, live timeline metadata, validators/tests, styles and the five software evidence documents including this report. No forbidden path is present. Existing canonical documents and historical assets remain identical to main.

12. **Checkpoints.** `ec9ddc3` — extract reusable device screen; `f2e612b` — reconcile software session and audio foundations; `dc9ed9e` — reconcile ios4 notification system; final checkpoint — reconcile shared media attachment and publishing. Each implementation phase passed relevant checks before its commit. The final media checkpoint also contains the cross-session capture regression fix and completed software-only audit evidence.

13. **Automated verification.** PASS: `npm run build`, `npm run test:seed`, `npm run test:public-twitter`, `git diff --check`, and every focused suite: `deviceAudio`, `notificationState`, `softwareFoundation`, `softwareSession`, `mediaAttachmentFlow`, `mediaKeyboardOwnership`, `mediaPublishing`. Seed validation exercises existing Messages/Facebook/Twitter state/submission, Camera, content and Foursquare contracts. The build retains the existing large-chunk advisory. Controlled capture/persistence mocks and server-rendered markup do not certify browser IndexedDB, JPEG pixels or audible playback.

14. **Safari handoff.** PENDING. The isolated QA server launch was declined; no live HTTP, browser-console, gesture or visual acceptance is claimed. To serve this worktree: `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`. Intended URL: `http://localhost:5174/?mediaAttachmentDebug=1&notificationDebug=1`. Check Messages Camera/library/cancel/draft/image send; Facebook Camera/library/draft/photo-only/caption publish; Twitter Camera/library/draft/text+photo; lock/foreground notification delivery, mute/no replay, and stable scene across requesters. The software audio gate can be exercised in the DEV console by importing `/src/audio/deviceAudio.ts` and calling `DeviceAudio.setMuted(true/false)`. Repeat with a second software session. No physical presenter is required.

15. **Confidence and HOLD.** This ports the project's established evidence-backed behavior; it is not a new historical audit. Existing iOS SMS assets/chrome and canonical continuity are preserved. Shared source chooser, pending-media treatment, third-party alerts and MMS details remain conservative RECONSTRUCTED surfaces. Exact MMS tail fidelity, Twitter media character cost/hosting details, Instagram October push, exact third-party alert pixels/sounds, granular notification deep links and final Safari visual/audio acceptance remain HOLD. No visual redesign or unrelated polish was performed.
